{
  "name": "cyber-portfolio",
  "version": "1.0.0",
  "description": "Modern cybersecurity-themed tech portfolio with React frontend and Node.js backend",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "server:dev": "cd backend && npm run dev",
    "client:dev": "cd frontend && npm start",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start"
  },
  "keywords": ["portfolio", "cybersecurity", "react", "nodejs", "express"],
  "author": "Tech Portfolio",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}